# analyzer-engine/ingestion/ingest.py

# CE FICHIER N'EST PLUS UN POINT D'ENTRÉE EXÉCUTABLE.
# TOUTE LA LOGIQUE A ÉTÉ MIGRÉE VERS LE FICHIER `cli.py` À LA RACINE DU PROJET.
# Cela résout les problèmes d'imports relatifs et fournit une interface
# de commande claire et unique pour l'application.

def placeholder():
    """
    Ce fichier est conservé pour la rétrocompatibilité des imports
    mais ne doit plus être exécuté directement.
    """
    print("Utilisez 'python3 cli.py ingest <chemin_fichier>' pour lancer l'ingestion.")